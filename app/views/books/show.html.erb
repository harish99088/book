 <div class="row">
 	          <div class="col-md-4">
					  <%= image_tag @book.book_img.url(:book_show), class: "book_show"%>
					</div>

					<div class="col-md-8">
				    <h2><%= @book.title %><h2>
					  <h3><%= @book.author %><h3>
						<p><%= @book.description %></p>
					</div>
		</div>


		<div class="row">
			<div class="col-md-4">
				<!--- reviews/ratings starts-->



			</div>
		</div>				

		<% if user_signed_in?%>
		<%if @book.user_id=current_user.id%>
    <%= link_to 'Back', root_path %>
		<%= link_to 'Edit',edit_book_path(@book)%>
		<%= link_to 'Delete', book_path(@book), method: :delete ,data:{cofirm:"Are You Sure?"}%>

		<%end%>
		<%end%>